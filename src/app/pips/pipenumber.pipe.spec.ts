import { PipenumberPipe } from './pipenumber.pipe';

describe('PipenumberPipe', () => {
  it('create an instance', () => {
    const pipe = new PipenumberPipe();
    expect(pipe).toBeTruthy();
  });
});
